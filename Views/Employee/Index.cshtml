@model IEnumerable<MVC_CRUD_EF_Core.Models.Employee>

@{
    ViewData["Title"] = "Index";
}

<h4>Employee Register</h4>
<hr />


<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FullName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmpCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Position)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OfficeLocation)
            </th>
            <th>
                <a asp-action="AddOrEdit" class="btn btn-outline-success"><i class="far fa-plus-square"></i> Employee</a>
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmpCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Position)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OfficeLocation)
            </td>
            <td>                
                <a asp-action="AddOrEdit" asp-route-id="@item.EmployeeId" class="btn btn-outline-warning"><i class="far fa-plus-square"></i> Edit</a>
                <a asp-action="Delete" asp-route-id="@item.EmployeeId" class="btn btn-outline-danger" onclick="return confirm('Are you sure to delete this record?')"><i class="far fa-plus-square"></i> Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>